interface lightSchema {
	id: string
	color: string
	state: boolean
}

interface alarmSchema {
	id: string
	state: boolean
	time: string
}

interface lampSchema {
	id: string
	state: boolean
}

interface timeSchema {
	id: string
	time: string
}